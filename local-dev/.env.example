# Local-only. Copy to .env. These secrets are throwaway — local preview only.
TAG=v2.8.3
SERVER_URL=http://localhost:3000
PG_DATABASE_PASSWORD=postgres
# openssl rand -base64 32
ENCRYPTION_KEY=dev_only_change_me
APP_SECRET=dev_only_change_me
